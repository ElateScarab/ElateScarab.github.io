Instructions to run:

Ensure Python 3.1X is installed
Open the AnimalShelter folder within VS Code
Select the Driver.py class
From the top ribbon, select Run -> Run without debugging -> Default Python configuration, if prompted
